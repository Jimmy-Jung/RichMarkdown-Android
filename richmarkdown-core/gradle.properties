# Author: JunyoungJung
# Date: 2026-09-15
# POM 값은 ASCII로 쓴다 — Java Properties는 Latin-1로 읽어 한글이 깨진다(실측).
POM_ARTIFACT_ID=richmarkdown-core
POM_NAME=RichMarkdown Core
POM_DESCRIPTION=Markdown + LaTeX parsing core (pure JVM): length-preserving math mask, math scanner, streaming tail, latest-wins worker
